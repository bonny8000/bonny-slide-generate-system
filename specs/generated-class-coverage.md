<!-- AUTO-GENERATED by scripts/compile_system.py. DO NOT EDIT. -->
# Class coverage — can each catalogued pattern be built from `assets/base.css`?

**13 of 44 patterns** can be built entirely from the shipped
stylesheet. **31** need a class that only exists inside their own example.

This matters because the agent builds with `assets/base.css`. When a catalogued pattern's CSS
lives only in its example, the agent cannot reuse it — it has to reinvent the component, which
is how visual consistency drifts even when intention routing is correct.

Machine-readable form: `system/class-manifest.json`.

## Patterns that need classes `base.css` does not define

| pattern | kind | missing classes |
|---|---|---|
| `as-is-to-be` | layout | `.ab-badge`, `.ab-col`, `.ab-grid`, `.anno`, `.asis`, `.device`, `.improve`, `.like`, `.preview`, `.problems`, `.thumbgrid`, `.thumbstrip`, `.tobe` |
| `citation-card` | component | `.connector` |
| `comparison-table` | component | `.ranklist`, `.rankrow` |
| `cta-buttons` | component | `.big`, `.brand`, `.btn`, `.chip`, `.ctas`, `.hero`, `.login`, `.menu`, `.primary`, `.secondary`, `.stats` |
| `delta-metric` | component | `.planpanel` |
| `editorial-explainer-stage` | layout | `.agenda`, `.family`, `.uiqa`, `.workflow` |
| `feature-showcase` | layout | `.anno`, `.area`, `.big`, `.scr` |
| `geo-map` | component | `.anno`, `.finding`, `.geo`, `.qrow` |
| `hero-radial` | layout | `.core`, `.radial`, `.sidecard` |
| `idea-evidence` | layout | `.bars`, `.big`, `.idea`, `.note` |
| `interview-affinity` | layout | `.insight`, `.pcol` |
| `level-slider` | component | `.pavatar`, `.pcard`, `.phead`, `.pid`, `.ptag` |
| `linked-circles` | layout | `.circ`, `.stadium` |
| `metric-card` | component | `.mcrow` |
| `painpoint-evidence` | layout | `.anno`, `.finding`, `.geo`, `.qrow` |
| `persona-cards` | layout | `.pavatar`, `.pcard`, `.phead`, `.pid`, `.ptag` |
| `positioning-matrix` | layout | `.ax-x`, `.ax-y`, `.map`, `.rank`, `.ranks`, `.target` |
| `problem-solution` | layout | `.pbadge`, `.problem`, `.solution` |
| `product-hero` | layout | `.big`, `.brand`, `.btn`, `.chip`, `.ctas`, `.hero`, `.login`, `.menu`, `.primary`, `.secondary`, `.stats` |
| `qual-quant-split` | layout | `.qcol`, `.sbcol` |
| `quote-bubble` | component | `.anno`, `.finding`, `.geo`, `.qrow` |
| `ranked-list` | component | `.ax-x`, `.ax-y`, `.map`, `.rank`, `.ranks`, `.target` |
| `research-flow` | layout | `.connector` |
| `results-grid` | layout | `.col`, `.hero-cap`, `.hero-num`, `.qband`, `.supp` |
| `service-flow` | layout | `.box`, `.branch`, `.dia`, `.node`, `.start` |
| `stat-bar` | component | `.qcol`, `.sbcol` |
| `taglist` | component | `.desc`, `.eyebrow-rule` |
| `timeline` | layout | `.axis`, `.bars`, `.phase` |
| `ui-mockup` | component | `.big`, `.brand`, `.btn`, `.chip`, `.ctas`, `.hero`, `.login`, `.menu`, `.primary`, `.secondary`, `.stats` |
| `use-case-cards` | layout | `.faint`, `.soft`, `.uc-badge`, `.uc-cap`, `.uc-card`, `.uc-illus`, `.uc-item` |
| `value-points` | layout | `.desc`, `.eyebrow-rule` |

## Missing classes shared by several patterns

A class several examples reach for is genuine shared vocabulary and is the highest-value
thing to promote into `base.css` first.

| class | patterns needing it |
|---|---:|
| `.anno` | 5 |
| `.big` | 5 |
| `.brand` | 3 |
| `.btn` | 3 |
| `.chip` | 3 |
| `.ctas` | 3 |
| `.hero` | 3 |
| `.login` | 3 |
| `.menu` | 3 |
| `.primary` | 3 |
| `.secondary` | 3 |
| `.stats` | 3 |
| `.finding` | 3 |
| `.geo` | 3 |
| `.qrow` | 3 |
| `.connector` | 2 |
| `.bars` | 2 |
| `.pavatar` | 2 |
| `.pcard` | 2 |
| `.phead` | 2 |
| `.pid` | 2 |
| `.ptag` | 2 |
| `.ax-x` | 2 |
| `.ax-y` | 2 |
| `.map` | 2 |
| `.rank` | 2 |
| `.ranks` | 2 |
| `.target` | 2 |
| `.qcol` | 2 |
| `.sbcol` | 2 |
| `.desc` | 2 |
| `.eyebrow-rule` | 2 |
